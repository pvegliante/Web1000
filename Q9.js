// Write a function which, given a 2-d array of strings,
    // returns the concatenation of all the strings.
var arr2 = [];
var x = arr;
function twodconcat(arr){
    for(r = 0; r < x.length; r++){
      for(c = 0; c < x[r].length; c++){
        arr2.push[r][c];
      }
    }
    return arr2;
}
